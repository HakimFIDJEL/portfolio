// resources/js/components/landing/transition-screen.tsx

interface TransitionScreenProps {
    active: boolean;
    setActive: (active: boolean) => void;
}

export default function TransitionScreen({ active, setActive }: TransitionScreenProps) {

} 